name: CI
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  Full-Pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout project
        uses: actions/checkout@v2
      - name: Gradlew Check
        run: | 
           chmod +x ./gradlew
           ./gradlew check
      - name: Deploy
        run: echo deploying!
